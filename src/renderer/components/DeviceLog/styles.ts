import { colors } from "../../config";

const styles = {
  xyPlot: {
    fontFamily: "'Source Sans Pro', sans-serif"
  },
  axes: {
    line: { stroke: colors.lightGrey },
    ticks: { stroke: colors.lightGrey },
    text: { stroke: "none", fill: colors.grey }
  }
};

export default styles;
